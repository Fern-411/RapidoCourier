# === bootstrap.yaml ===
spring:
  application:
    name: servicio-paquetes
  cloud:
    vault:
      host: ${VAULT_HOST:localhost}
      port: 8200
      scheme: http
      token: root
      kv:
        enabled: true
        backend: secret
        default-context: rapidocourier/paquetes